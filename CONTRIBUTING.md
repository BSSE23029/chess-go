# Contributing

The contribution guide lives in [`docs/contributing.md`](docs/contributing.md).
It covers architecture boundaries, verification gates, testing expectations,
environment-backed configuration, and the local commit workflow.

Before submitting a change, run:

```console
make verify
```
