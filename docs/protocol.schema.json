{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://chess-go.example/schema/protocol-v1.json",
  "title": "chess-go protocol v1 envelope",
  "type": "object",
  "additionalProperties": false,
  "required": ["version", "type", "payload"],
  "properties": {
    "version": {"const": 1},
    "type": {
      "enum": [
        "match.create", "match.join", "match.snapshot", "match.move",
        "match.move.accepted", "match.resign", "match.draw.offer", "error"
      ]
    },
    "request_id": {"type": "string"},
    "payload": {"type": "object"}
  },
  "$defs": {
    "create_match": {
      "type": "object",
      "additionalProperties": false,
      "required": ["match_id"],
      "properties": {
        "match_id": {"type": "string", "minLength": 1},
        "initial_fen": {"type": "string"},
        "player_id": {"type": "string"},
        "color": {"enum": ["white", "black", "spectator"]},
        "clock_millis": {"type": "integer", "minimum": 0},
        "increment_millis": {"type": "integer", "minimum": 0}
      }
    },
    "join_match": {
      "type": "object",
      "additionalProperties": false,
      "required": ["match_id", "player_id", "color"],
      "properties": {
        "match_id": {"type": "string", "minLength": 1},
        "player_id": {"type": "string", "minLength": 1},
        "color": {"enum": ["white", "black", "spectator"]}
      }
    },
    "move": {
      "type": "object",
      "additionalProperties": false,
      "required": ["match_id", "player_id", "sequence", "position_hash", "uci"],
      "properties": {
        "match_id": {"type": "string", "minLength": 1},
        "player_id": {"type": "string", "minLength": 1},
        "sequence": {"type": "integer", "minimum": 0},
        "position_hash": {"type": "integer", "minimum": 0},
        "uci": {"type": "string", "minLength": 4}
      }
    },
    "snapshot": {
      "type": "object",
      "additionalProperties": false,
      "required": ["match_id"],
      "properties": {
        "match_id": {"type": "string", "minLength": 1},
        "player_id": {"type": "string"}
      }
    },
    "match_snapshot": {
      "type": "object",
      "additionalProperties": false,
      "required": ["match_id", "sequence", "position_hash", "fen", "turn", "result", "spectators"],
      "properties": {
        "match_id": {"type": "string", "minLength": 1},
        "sequence": {"type": "integer", "minimum": 0},
        "position_hash": {"type": "integer", "minimum": 0},
        "fen": {"type": "string"},
        "turn": {"enum": ["white", "black"]},
        "result": {"enum": ["*", "1-0", "0-1", "1/2-1/2"]},
        "draw_offered_by": {"type": "string"},
        "spectators": {"type": "integer", "minimum": 0},
        "white_time_millis": {"type": "integer", "minimum": 0},
        "black_time_millis": {"type": "integer", "minimum": 0},
        "increment_millis": {"type": "integer", "minimum": 0},
        "clock_running": {"type": "boolean"}
      }
    },
    "move_accepted": {
      "type": "object",
      "additionalProperties": false,
      "required": ["match_id", "sequence", "position_hash", "fen", "uci", "result"],
      "properties": {
        "match_id": {"type": "string", "minLength": 1},
        "sequence": {"type": "integer", "minimum": 0},
        "position_hash": {"type": "integer", "minimum": 0},
        "fen": {"type": "string"},
        "uci": {"type": "string", "minLength": 4},
        "result": {"enum": ["*", "1-0", "0-1", "1/2-1/2"]},
        "white_time_millis": {"type": "integer", "minimum": 0},
        "black_time_millis": {"type": "integer", "minimum": 0},
        "increment_millis": {"type": "integer", "minimum": 0},
        "clock_running": {"type": "boolean"}
      }
    },
    "action": {
      "type": "object",
      "additionalProperties": false,
      "required": ["match_id", "player_id"],
      "properties": {
        "match_id": {"type": "string", "minLength": 1},
        "player_id": {"type": "string", "minLength": 1}
      }
    }
  }
}
